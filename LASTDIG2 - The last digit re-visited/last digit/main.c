#include <stdio.h>
#include <stdlib.h>

int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        printf("%d",sizeof(long long int));
        long long index;
        int base;
        scanf("%d %ld",&base,&index);

    }
}
